# Traceability

The canonical specification package is present at the repository root (version `5.2.3`,
schema `5`). Gate 0 runtime evidence and deterministic Gates 1, 3 and 4 are complete; the owner-
approved strict adapter has a green linked Gate 0 probe and B04 business pilot. Ignored deployment/
live/browser records remain under `runtime/` until the later evidence freeze.

During implementation, maintain a compact table with these columns:

| Requirement | Implementation | Verification | Status |
| --- | --- | --- | --- |
| Authoring: ordinary campaign | `/campaigns/new`, server catalog, no implicit `case_id`; dashboard separates campaigns/examples/tests | Authoring API/unit tests; desktop/narrow Playwright | Providerless green |
| Authoring: existing/new product | Read-only catalog plus versioned custom synthetic fact-card overlay; server IDs and PII/URL/type validation | Happy/edge API tests; grounded custom-product browser flow | Implemented |
| Authoring: editorial references | Seven sanitized briefs; API excludes `saved_draft`; explicit non-live marker | Asset integrity and wizard-prefill regressions | Implemented; not live/release evidence |
| Authoring: channel copy contract | Channel-specific fact selection; independent SMS/e-mail; 2–4 e-mail sections; coherent Russian skill/projection; policy-derived UCS-2 SMS bound; allowed-surface numeric matching with exact QA17 evidence normalization | QA/revision/schema tests; byte-equal prompt check; B01–B15 replay 15/15; preserved `…-06` QA12 pilot; run `…-10` schema + pilot + sole basket; final 431 ordinary / 11 contract tests | Engineering green; live basket 3/7 mechanical and 0 strong, gaps recorded |
| Authoring: responsive cancel | Status row/grid containment and narrow wrapping without changing cancel semantics | 1920×1080 and 390×844 Playwright, screenshots and overflow assertions | Implemented and browser-smoked |
| Authoring: physical accounting | Fresh goal-only ledger; official exact-model price pinned immutably per run under conservative ceilings; actual serialized request at 2 chars/token; 25% reservation; one inflight; exact/unknown reconciliation; one bounded transient retry may coexist with the first retained bound; ledger-only supplemental GID | Price drift/admission/retry tests; rebuilt UID 10001 probe; full 600-second metadata polls; goal exact 1,852,109 tokens / `$1.344667858200`, retained 255,289 / `$0.312184990000`; 51 exact + 2 unknown physical requests | Engineering/accounting green under 50M / `$150`; two durable unknowns reported, not release evidence |
| Canonical specification imported | Seven root files, one canonical copy | Hash/root preflight and complete read | Complete |
| G0 pinned runtime | `ouroboros/ouroboros.lock`, pinned Dockerfile, ephemeral runtime launcher | `make bootstrap`; source/hash/version probe; `make source-audit` | Complete |
| G0 instruction activation | Reviewed `SKILL.md` body, byte-equal generated prompt, `adapter_injected` task admission | `make skill-contract`; decoded first-provider-request no-forward lock | Complete |
| G0 exact tool isolation | Production inventory lock; generated 89-name denylist; registry execution guard | No-forward provider seam and live receipts equal the two prefixed tools | Complete |
| G0 MCP contract | Settings-managed private streamable HTTP server; timeout 5; bearer boundary | Settings/status readback, discovery, auth/payload and Compose contract tests | Complete |
| G0 credential boundary | External `0600` key; Ouroboros-only read-only mount/process export; guarded persistence | `scripts.preflight`, process/settings inspection, `scripts.image_security` | Complete |
| G0 budget control | External operator profile, unique markers, project ledger, no-switch guards; CF-RP-011 admits exact provider/model drift only as failed/non-evidence with matching observed identities and checksum quarantine | `make test-budget`; route-drift positive/tamper regressions; Review-05 exact recovery and linked Review-06 | Deterministic guard and replacement review green |
| G0 post-task accounting | Runtime adapter ledger plus app MCP timestamps and immutable draft hash | Linked live run `gate0-live-probe-20260711-02`; `make gate0-evidence` | Complete |
| G0 physical provider accounting | CF-RP-005 captures safe OpenRouter generation IDs; CF-RP-008 also records content-free prompt UTF-8 byte upper bounds and actual configured max output, treats no-ID pre-generation failures as zero-reservation anomalies, polls generation metadata for at most 600 seconds and bounds only an unresolved individual request | Runtime hook/redaction tests; successful request after no-ID 429; orphan projection; delayed/404 bounded poll; immutable ledger-prefix, pricing, estimate and six-attempt budget regressions | Deterministic implementation green; replacement review pending |
| G0 latency/worker decision | Unmodified upstream baseline retained | 12.193 s user-visible; 16.200 s occupancy; summary measured; no core patch | Complete |
| G0 architecture/spec lock | Backend uses only private Ouroboros Task API; machine constants projection | `make architecture`; `make spec-drift`; contract tests | Complete |
| G0 source/license/private audit | Exact source/archive/requirements hashes; MIT discrepancy notice; ignore boundaries | `make source-audit`; image and Git tracking scans | Complete |
| Legacy reuse | Rewritten typed domain/MCP ideas only; private runtimes/assets quarantined | `docs/LEGACY_REUSE.md`; source/architecture audits | Complete for Gate 0 |
| G1 synthetic catalog/cases | `data/synthetic/` fixed seed, six products, nine personas, policies/history and exact B01-B15 | Catalog safety/schema tests; Gate 1 plus full reference basket assertions | Complete |
| FR-01 brief promotion | Versioned incomplete drafts, typed questions, deeply immutable ready snapshot and input hash | Promotion/store/API tests; missing CTA returns domain `NEEDS_INPUT`; zero LLM calls | Complete for G1 |
| FR-02 context | Strict fact card/ledger/persona/policies/concepts, deterministic content plan and stable context hash | B04 duration and optional-concept assertions; B06 disclaimer selection | Complete for G1 |
| FR-03/04 artifacts | Typed SMS/e-mail builder, non-URL fact sections, top-level CTA rendering, GSM-7/UCS-2 metrics and sanitized HTML | Boundary/emoji/XSS/no-CTA-section tests; authenticated B04 production-container smoke | Complete for G1 |
| FR-05/06 grounding and QA | Independent actual-claim extraction, evidence/path/source matching, exact initial placement and versioned 22-check registry; CF-RP-014 closes assertion sources to selected facts/concepts/required phrases and enumerates real zero-based initial evidence paths | Unsupported 99%, false/nonexistent-path, duplicate-URL, missing-body-evidence, grounded-extra-path, sentence-capitalization and case-changed-URL regressions; B01/B09 schema topology tests; Basket-04 rejection artifacts | Complete deterministically; CF-RP-014 live requalification pending |
| FR-09 state/approval/export | SQLite immutable versions, current-pointer invalidation, human-role/test-only approval and checksummed ZIP | Restart/idempotency/blocker/hash/invalidation/export tests; archive checksum verification | Complete for G1 |
| G1 REST/CLI flow | Versioned cases/campaign/brief/answer/run/package/approval/export routes and `scripts.gate1` | REST integration tests; `make verify-gate1` (90 tests, zero provider calls) | Complete |
| G2 campaign MCP boundary | Current hashed context authorization; atomic agent draft/package QA; consumed/rejected/closed late-write fence | Real-context MCP integration, QA rejection and idempotent receipt tests | Complete deterministically |
| G2 lossless context transport | CF-RP-002 registers only the P0/P1 typed factory context tools for a bounded 80,000-character result and records metadata-only raw/visible length plus JSON-validity telemetry | Runtime-launcher regressions preserve a 16k JSON context, retain the 15k default for unrelated tools and truncate above 80k; pinned v6.61.4 B04 probe preserves all 15,456 characters as valid byte-equal JSON; hosted campaign-authoring run read full context and saved a live package | Deterministic/pinned-source probe and hosted Railway GLM-5.2 authoring flow green |
| G2 typed safety verdict | CF-RP-003 replaces generic JSON intent only for GLM safety calls with a strict closed verdict schema; CF-RP-012 gives exact GLM safety 20 seconds; CF-RP-013 gives its enclosing tool executor 30 seconds while retaining full safety/parser/fail-closed behavior | Basket-03 preserves B05/B08 eight-second outer-timeout artifacts; Review-08/bootstrap lock requires GLM main/safety/tool 10,240/20/30; core 20/20 | Replacement review/core green; linked B01/basket pending |
| G0 current prompt lifecycle | One coherent Russian source, byte-equal projection, context-exact array counts/path enums, closed assertion sources, aligned field schemas, executable review lifecycle and fail-closed admission | CF-RP-014 prompt `c2303a08…`; Review-09 exact 126,908 / $0.18105257; lock `cbe60a5b…`; initial constants, literal zero-based paths, null context version, declared revision diffs and terminal ASCII receipt | Review/bootstrap and providerless core 20/20 green; linked B01 pending |
| G0 model-provider qualification package | Sanitized synthetic cases, operation schemas, strict projections, offline invariants, failure taxonomy and mechanical/qualitative rubrics | 27 JSON files parse; 30/30 declared checksums match; preliminary external GLM observations have no imported payload/usage receipt | Offline/conceptual only; guarded exact-route probe pending |
| G0 current live transport | Strict private probe fixture with business-processor dispatch exclusion | Probe `-03` preserved failed/accounted; linked `gate0-live-probe-20260711-04` all checks green at 13.910/16.591 s; `make verify-gate0` | Complete |
| G2 Task adapter/lifecycle | Exact constraints/denylist payload; persistent run/events; one active; SSE resume; pinned release proof; bounded cancel/reconcile; CF-RP-012 enters ordinary no-tool finalization only from a real persisted-save receipt; CF-RP-013 stops the unexecuted suffix of a save-containing batch after that receipt | Positive/negative receipt, one-shot provider fall-through, persisted multi-save suppression and non-persisted batch regressions; pinned probe; Review-08/core green | Replacement review/core green; linked B01/basket pending |
| Owner-controlled provider retry extension | Typed Task API transport failures and allowlist classifier; one logical run with durable max-two `RunAttemptRow` audit; first-task locate/cancel/release fence; attempt-scoped MCP authorization; identical request/context/provider/model/hash/tool identity; atomic submission claim; attempt-aware API/UI/export; release flag off | `tests/integration/test_controlled_provider_retry.py` maps acceptance cases 1–15 (success, 429/503, deadline, ambiguous submit, unconfirmed release, saved-result recovery, permanent classes, cancel, exhaustion, restart/concurrency, bounded delay, flag-off, identity and ledger/export); Task API/MCP/settings unit regressions; `tests/e2e/controlled-provider-retry.spec.ts` maps case 16 for Russian success/failure, screenshots, console and overflow; `make verify-core` 21/21 and separate `make security`, provider calls 0; `reports/controlled-provider-retry-20260717.md` | `CONTROLLED_PROVIDER_RETRY_ENGINEERING_GREEN_FLAG_OFF`; canonical/Compose default remains off; Railway enables the bounded retry; hosted live qualification recovered one transient 600-second deadline on attempt two |
| G2 genuine Ouroboros slice | Current campaign lifecycle, one-pass reviewed instruction and strict operation-specific output schema | Failed B04 `-01`..`-04` preserved/accounted; linked `gate2-b04-pilot-20260712-05` passed two tools, QA 22/22 and complete usage | B04 live slice complete; full basket pending |
| G2 strict provider contract | Approved project-scoped strict adapter for exact two MCP functions; no non-strict fallback | Adapter/application tests; zero-issue assessment; production no-forward lock records adapter/image/schema hashes, non-target byte equality and zero `$ref` siblings; Gate 0 `-05` preserved failed and linked `-06` passed | Live requalification complete for adapter image |
| FR-07 feedback and revision | Immutable feedback records; operation-scoped revision context; strict patch/merge boundary; full QA on a new package version | B01 API/unit/MCP tests; changed/protected/duplicate path regressions; `make verify-gate3` | Complete deterministically |
| FR-08 governed rules | Closed typed proposal; target/regression/out-of-scope tests; human test-only approval/rejection/rollback; immutable version and active pointer | Rule unit/API/MCP tests; B03 exact rule evidence and channel scope; rollback/future-context checks | Complete for B01/B03 Gate 3 slice |
| G3 managed operation contract | Initial, revision and rule operations share the exact two MCP tools and persist only the matching typed result | Operation-filtered snapshot/save integration tests; six-case rule test set; zero provider calls | Complete deterministically |
| G4 four-screen UI | URL-addressable Cases, Campaign Workspace, Evaluation and Diagnostics; shared documented design system | npm lint/typecheck/build; direct reload and authenticated browser flows | Complete |
| G4 REST/SSE and previews | Typed safe dashboard/workspace/evaluation/diagnostics read models; real mutations; normalized SSE; SMS/e-mail/facts/claims/QA/diff/rule/trace views | Presentation API happy/empty tests; nine-scenario Playwright matrix | Complete deterministically |
| G4 approval/export safety | Exact package/rule hash dialogs; E2E `test_only`; separate export/download; no-send copy | Browser blocker, rule approval/rollback and real ZIP download scenarios | Complete deterministically |
| G4 responsive/accessibility | Semantic landmarks/labels/dialogs/tables, skip/focus states, reduced motion and contained narrow tables | 1920x1080 matrix; focused 390x844 focus/overflow smoke; inspected screenshots | Complete |
| G4 golden flow | B01 needs-input → v1 → feedback/v2/diff → tested rule → B03 exact rule evidence → rollback | Five consecutive independent Playwright tests; five traces and screenshots; no console/page errors | Complete in `deterministic_template`; live evidence remains separate |
| FR-10 evaluation presentation | Current measured DB slice, explicit non-frozen state, separated modes, operator review status and working JSON report link | Evaluation API/browser tests; dashboard reports actual current coverage | UI complete; frozen 15+5 evidence is Gate 5 |
| G5 business reference basket | Versioned exact B01-B15 expectations and hard assertions; deterministic full workflow including feedback/rule/rollback/revision | `make eval-replay`; 15/15, zero provider calls, release false | Complete as non-live oracle |
| G5 chaos isolation | Dedicated bounded X01-X05 runner with explicit non-normal metrics and zero-call accounting; cancel is immediately re-observed | Ten consecutive X01–X05 suites; checkpoint `b268db2` full core includes five outcomes under 30 s | Complete deterministically |
| G5 security hardening | Tree/history secret scan; synthetic/artifact PII; no-send/archive, dependency/license, architecture and Compose checks | `make security`; all finding counts zero | Complete for current tree |
| G5 immutable evidence | Frozen-live-only builder for PDF/JPG/HTML/CSV/JSON/JSONL, per-case/demo/trace/browser artifacts, checksums and marker | Evidence fixture/tamper tests; real offline Playwright PDF/JPEG smoke; missing live ID fails closed | Pipeline complete; actual frozen live source pending |
| G5 qualitative packet readiness | Six preselected live case IDs, immutable output packets, eight-item rubric and blank human form schema | Tests require six packets and null reviewer/scores/comments/aggregate | Pipeline complete; actual live packets pending |
| G5 guarded live basket | Strict-lock/readiness/clean-commit admission; unique capped sequential B01–B15 runner; exact rows survive failure; recoverable cases continue; unresolved generation estimates stay outside provider ledger | Basket-04 passed 12/15, recorded complete exact 1,201,126 / $0.61784729 with zero estimates and preserved B01/B03/B09 quarantine; B05/B08 timeout class green | Failed/non-evidence preserved; CF-RP-014 replacement qualification pending |
| Owner-authorized VPS GLM basket profile | Exact OpenRouter GLM route, two-tool isolation, 10,240-token main turns, no fallback; 100M/$200 post-prefix window; confirmed usage plus bounded per-call estimates; six directed attempts/class; CF-RP-011 route admission; CF-RP-012/013 lifecycle and CF-RP-014 grounding boundaries | B01-07 green; Basket-04 12/15; Review-09 exact 126,908 / $0.18105257; lock image `aac1e6…`; core 20/20 with zero provider calls | `P0_GLM_CF_RP_014_CORE_GREEN`; linked B01/readiness/basket pending |
| G5 dependency/license inventory | `scripts/license_scan.py`; four authoritative locks, image metadata and version-bound platform overrides | `make license`; 330 components, lock hashes, zero prohibited licenses | Complete deterministically |
| G5 frozen presentation | Read-only evidence mount/catalog; frozen-first UI selection; allowlisted report streaming; exact-two-tool diagnostics | Presentation API fixture/tamper tests; frontend lint/typecheck; frozen/no-frozen E2E branches | Complete deterministically; actual frozen source pending |
| Release isolated smoke | Unique providerless app/gateway Compose project, B04/export/restart flow and unconditional cleanup | `make smoke`; 21-file ZIP, 22 QA checks, zero provider calls | Complete deterministically |
| Hosted Railway demo profile | One-service public Caddy plus private cookie-session auth/FastAPI/Ouroboros processes; exact OpenRouter GLM-5.2 route; hosted live/human UI profile; typed destructive reset; deployment-bound no-forward admission | Auth/CSRF/rate-limit and secret-boundary unit tests; frontend lint/typecheck/build; 21 targeted Railway/profile tests; authenticated hosted browser authoring, QA, approval and export smoke; actual GLM-5.2 package inspected | Green on Railway commit 2925715; live authoring completed after one controlled retry; QA 22/22; no external send |
| Release isolated browser matrix | Unique providerless Compose project and generated auth/port; pinned Playwright 15 scenarios, golden ×5 and narrow smoke | `make e2e`; result marker, 8+ screenshots, exactly five traces, zero provider calls | Complete deterministically |
| Release verification hierarchy | Explicit mutation-guarded core allowlist; clean commit/live/evidence/demo/clean-clone implementation gate; human-only submission layer | Verify/package/clean-clone tests and dry runs; `make verify-core` 20/20 with protected state unchanged | Complete deterministically; frozen implementation inputs pending |
| Final providerless VPS verification | Linked-worktree-aware preflight; complete-volume reviewed-state seed; exact-SHA disposable README startup | Clean checkpoint `c9ba1a3`; core 20/20; 329 project tests; Playwright 27/27 and golden 5/5; separate security; exact-SHA startup/B01/reset; protected state unchanged; provider calls 0 | Providerless green; canonical frozen implementation and human submission inputs remain pending |
| Demo readiness | Providerless scoped reset; strict read-only check; unique capped current B04 canary excluded from metrics | Demo/admin tests; strict adapter now passes doctor; frozen release and current canary still required | Pipeline complete; release-ready record pending |
| Submission package | Fail-closed operator schemas, review/approval/sign-off/rehearsal/video/link validation and checksummed offline package | `make package-submission-dry-run`; negative schema/authority tests | Pipeline complete; genuine human records pending |
| Rule proposal provenance | Author-only wire draft; backend-bound feedback/scope/targets/proposal and candidate versions; unchanged canonical validator | Domain schema, deterministic materialization and real MCP persistence tests | Complete deterministically; live rule output pending strict runtime |
| Submission artifact binding | Frozen packet SHA-256 plus manifest rule/package approval targets; allowlisted operator-record copy | Evidence tamper and operator-binding regressions | Complete deterministically; genuine human records pending |
| SQLite durability | Shared Workflow/MCP engine policy: WAL, 5 s busy timeout, foreign keys, normal sync; explicit parent flush ordering | SQLite pragma regression plus workflow/MCP persistence suites and container readback | Complete deterministically |
| VPS resource/log bounds | Canonical Compose restart, CPU/memory/PID limits and 3 × 10 MiB per-service log rotation | Rendered Compose contract plus negative drift test | Complete deterministically |
| DB/rules/evidence backup | Online SQLite snapshot, rule-table/schema/FK integrity, redacted lock, immutable evidence, checksummed mode-0600 ZIP and guarded retention=7 | Backup build/tamper/prune tests; `make backup-check`; final verifier binds commit/evaluation | Pipeline complete; release backup follows frozen evidence |

| MVP confirmed result presentation | Checksum-validated projection of exactly 10 passed live Basket-03 cases; Russian read-only outputs screen; full catalog unchanged | Presentation API exact-ID/tamper tests; frontend lint/typecheck/build; 12-scenario Playwright presentation suite; hosted desktop/mobile smoke | Green locally and on Railway commit bc0d731 |
| Final demo operation UX | Server-derived logical operation view plus one reusable initial/revision/rule card; polling/SSE reload recovery; synchronous command mutex; one rule CTA and one cancel; Russian-first concepts/reasons; МСК display; clean product labels; ordinary new-campaign path; persistent global safety boundary | `tests/unit/test_operation_presentation.py`; presentation API regressions; `tests/e2e/final-ui-polish.spec.ts`; full `make e2e` 36 passed / 2 controlled-profile skips, golden 5/5, 18 screenshots / 5 traces / zero provider calls; controlled-retry 2/2; inspected 1920×1080 and 390×844 running/retry/reload/success/failure/cancel/reduced-motion states; final `make verify-core` 21/21 and `make security` green with protected state unchanged | `FINAL_UI_POLISH_GREEN_PROVIDERLESS` |

Populate requirement identifiers only from the canonical specification. Do not invent acceptance evidence or mark product work complete before the corresponding implementation and verification exist.

## Соответствие новым критериям Project Results (финальная ревизия, 2026-07-16)

| Критерий (вес) | Артефакты | Статус |
| --- | --- | --- |
| Отчёт о результатах MVP (20%) | `docs/PROJECT_RESULTS_REPORT.md`; `docs/Фабрика коммуникаций (осн. этап).pdf` | Отчёт и итоговая презентация готовы |
| Применение ГигаАгента (10%) | Раздел 3 отчёта; экран «Диагностика»; архитектурный скан no-backend-LLM | Подтверждено артефактами и тестами |
| ДЕМО-видео <3 мин (30%) | README и `docs/JURY_GUIDE.md` содержат место для публичной ссылки | Запись с голосом готовит владелец; ссылка добавляется после загрузки |
| Документация и код (10%) | `README.md` (4 шага); `https://github.com/akimovdspb/SerenaSK_SberHack_2026` | Подготовлен очищенный публичный репозиторий |
| Результаты на примерах (20%) | Вкладка «Результаты» (10 живых кейсов); `reports/basket03-mvp-testing/` | Подтверждено сохранёнными артефактами |
| Качество материалов (10%) | `docs/TERMINOLOGY.md`; presentation-тесты; единый русский слой UI/доков | Согласовано в этой ревизии |
